{% extends 'base.html.twig' %}

{% block body %}
<div class="tarif bg-gris py-5">     
    <h1 class="heading-2 mb-1 text-center">Résumé du pack choisi</h1>
    <a href="{{ path('app_pack_buy') }}" class="text-muted" >(Retour au choix du pack)</a>      
    <div class="container mx-auto mt-5">
        <div class="tarif-left col-md-6 col-sm-12 mt-2">
            <h4 class=" mb-4">Nom: {{ pack.name }} <span class="prix-tarif">Prix: {{ pack.price }} €</span></h4>
            <p>Le pack {{ pack.name }} vous permet d'utiliser le calculateur {{ pack.quantity }} fois.</p> 
            <p>Le calculateur vous délivrera la liste exhaustive des matériaux nécessaires à la réalisation de la structure que vous avez choisie 
            à l'écran et sous un format PDF téléchargeable.
            </p>
            <div class="d-flex justify-content-center py-5 align-items-center">
                <form action="{{ path('app_account_payment', {'packId': pack.id}) }}" method="POST">
                <button class="btn btn-primary btn-lg"type="submit">Commander avec Stripe</button>
                </form>
            </div>
        </div>
        
            
        
    </div>    
</div>    
{% endblock %}
    